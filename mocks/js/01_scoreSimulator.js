/**
 * 入力スコアから予想スコアを計算する
 */
function calcScore() {

}

/**
 * レベルからレベルボーナスを計算する
 */
function calcLevelBonus() {
    let bonusRate = 0;

    if (level <= 10) {
        bonusRate = level / 2;
    } else if (level <= 20) {
    }
}